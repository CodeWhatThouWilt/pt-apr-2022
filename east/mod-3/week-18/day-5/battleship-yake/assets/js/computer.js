export default class ComputerPlayer {
	constructor(board) {
		this.board = board;
        
	}

	makeMove() {}

	getMoveCoords() {
		const boardRows = this.board.numRows;
		const boardCols = this.board.numCols;
		const row = Math.floor(Math.random() * boardRows);
		const col = Math.floor(Math.random);

        if (this.moves)
	}
}
